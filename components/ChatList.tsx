import {View, Text} from "react-native";
import React, { ReactElement } from "react";

const ChatList = (): ReactElement | null => {
    return (
        <View>
            <Text>
                No Chats Ongoing
            </Text>
        </View>
    );
};

export default ChatList;